apiVersion: v1
kind: Service
metadata:
  name: traefik-dashboard-service
  namespace: kube-system

spec:
  type: ClusterIP
  selector:
    app.kubernetes.io/instance: traefik
  ports:
    - name: http
      port: 9000
      targetPort: 9000
