apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: mealie-data-pvc

spec:
  storageClassName: longhorn-durable
  accessModes:
    - ReadWriteOnce

  resources:
    requests:
      storage: 512Mi

  volumeName: user-apps-mealie-data-pv
