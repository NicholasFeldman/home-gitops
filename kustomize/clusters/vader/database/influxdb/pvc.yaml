apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: influxdb-data-pvc

spec:
  storageClassName: longhorn-durable
  accessModes:
    - ReadWriteOnce

  resources:
    requests:
      storage: 4Gi

  volumeName: database-influxdb-data-pv
