apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: heimdall-config-pvc

spec:
  storageClassName: longhorn-durable
  accessModes:
    - ReadWriteOnce

  resources:
    requests:
      storage: 256Mi

  volumeName: user-apps-heimdall-config-pv
