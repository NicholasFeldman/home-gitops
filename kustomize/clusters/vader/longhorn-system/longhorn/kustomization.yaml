apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

resources:
  - ingressroute.yaml

secretGenerator:
  - name: aws-secret
    namespace: longhorn-system
    envs:
      - longhorn.env

generatorOptions:
  disableNameSuffixHash: true
