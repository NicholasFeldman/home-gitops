apiVersion: gateway.networking.k8s.io/v1beta1
kind: HTTPRoute
metadata:
  labels:
    app.kubernetes.io/instance: spoolman
    app.kubernetes.io/managed-by: Helm
    app.kubernetes.io/name: spoolman
    argocd.argoproj.io/instance: skywalker-application
    helm.sh/chart: app-template-3.1.0
  name: spoolman
  namespace: application
spec:
  hostnames:
    - spoolman.feldman.in
  parentRefs:
    - group: gateway.networking.k8s.io
      kind: Gateway
      name: feldman-in
      namespace: cluster
  rules:
    - backendRefs:
        - group: ''
          kind: Service
          name: spoolman
          namespace: application
          port: 8000
          weight: 1
