apiVersion: argoproj.io/v1alpha1
kind: Application

metadata:
  name: kenobi-media-jellyfin
  namespace: argocd

spec:
  destination:
    namespace: media
    name: in-cluster
  project: kenobi
  source:
    chart: app-template
    repoURL: http://bjw-s.github.io/helm-charts/
    targetRevision: 0.2.*
    helm:
      releaseName: jellyfin
      values: |-
        image:
          repository: jellyfin/jellyfin
          tag: 10.8.5
        env:
          TZ: America/New_York
        service:
          main:
            ports:
              http:
                port: 8096
        persistence:
          config:
            enabled: true
          media:
            enabled: true
            type: nfs
            server: 192.168.40.111
            path: /volume1/Media
            mountPath: /media
            readOnly: true
