apiVersion: networking.istio.io/v1beta1
kind: VirtualService

metadata:
  name: adguard

spec:
  gateways:
    - istio-ingress/feldman-in
  hosts:
    - adguard.feldman.in
  http:
    - match:
        - uri:
            prefix: /
      route:
        - destination:
            host: adguard
            port:
              number: 80
