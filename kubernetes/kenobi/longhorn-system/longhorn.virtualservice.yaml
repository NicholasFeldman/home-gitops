apiVersion: networking.istio.io/v1beta1
kind: VirtualService

metadata:
  name: longhorn

spec:
  gateways:
    - istio-system/feldman-in
  hosts:
    - longhorn-new.feldman.in
  http:
    - match:
        - uri:
            prefix: /
      route:
        - destination:
            host: longhorn-frontend
            port:
              number: 80
