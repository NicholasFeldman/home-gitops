apiVersion: storage.k8s.io/v1
kind: StorageClass
metadata:
  name: nfs-hoth
provisioner: nfs.csi.k8s.io
parameters:
  server: hoth.feldman.in
  share: /volume1/Kenobi Storage
reclaimPolicy: Retain
volumeBindingMode: Immediate
mountOptions:
  - nconnect=8
  - hard
  - noatime
