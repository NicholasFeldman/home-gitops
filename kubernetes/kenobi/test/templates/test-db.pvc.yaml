apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: test-db
  namespace: test
spec:
  storageClassName: nfs-client
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 5Gi
