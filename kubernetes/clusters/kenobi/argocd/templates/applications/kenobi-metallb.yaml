apiVersion: argoproj.io/v1alpha1
kind: Application

metadata:
  name: kenobi-metallb
  namespace: argocd

spec:
  destination:
    namespace: metallb
    name: in-cluster
  project: kenobi
  source:
    repoURL: git@github.com:NicholasFeldman/kubernetes-config.git
    targetRevision: HEAD
    path: kubernetes/clusters/kenobi/metallb
  syncPolicy:
    automated: {}
