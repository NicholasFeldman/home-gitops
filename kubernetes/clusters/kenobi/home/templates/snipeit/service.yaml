apiVersion: v1
kind: Service
metadata:
  name: snipeit
spec:
  selector:
    app.kubernetes.io/instance: kenobi-home
    app.kubernetes.io/name: snipeit
    app.kubernetes.io/version: 5.4.1
  type: ClusterIP
  ports:
    - protocol: TCP
      port: 80
      targetPort: 80
