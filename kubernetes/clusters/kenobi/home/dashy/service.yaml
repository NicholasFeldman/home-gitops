apiVersion: v1
kind: Service
metadata:
  name: dashy
  namespace: home
spec:
  selector:
    app.kubernetes.io/instance: kenobi-home
    app.kubernetes.io/name: dashy
    app.kubernetes.io/version: 2.0.3
  ports:
    - protocol: TCP
      port: 80
      targetPort: 80
