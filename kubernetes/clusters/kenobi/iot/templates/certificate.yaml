apiVersion: cert-manager.io/v1
kind: Certificate

metadata:
  name: iot-tls

spec:
  dnsNames:
    - node-red.feldman.in
    - home-assistant.feldman.in
  issuerRef:
    kind: ClusterIssuer
    name: letsencrypt-prod
  secretName: iot-tls
