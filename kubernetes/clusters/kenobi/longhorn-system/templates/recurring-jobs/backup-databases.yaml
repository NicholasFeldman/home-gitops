apiVersion: longhorn.io/v1beta1
kind: RecurringJob

metadata:
  name: backup-databases
  namespace: longhorn-system

spec:
  concurrency: 2
  task: backup
  retain: 10
  cron: 0 2 ? * MON,WED,FRI
  groups:
    - database
