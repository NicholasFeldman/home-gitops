apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

bases:
  - ../../../../bases/database/postgres
  - sealed-secret.yaml

patches:
  - path: deployment.patch.yaml
    target:
      kind: Deployment
  - path: service.patch.yaml
    target:
      kind: Service
