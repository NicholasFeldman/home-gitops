apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

resources:
  - ../../../../bases/application/keycloak
  - gateway.yaml
  - sealed-secret.yaml

patchesStrategicMerge:
  - stateful-set.patch.yaml
