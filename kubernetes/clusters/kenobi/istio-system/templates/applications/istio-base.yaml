apiVersion: argoproj.io/v1alpha1
kind: Application

metadata:
  name: kenobi-istio-system-istio-base
  namespace: argocd

spec:
  destination:
    namespace: istio-system
    name: in-cluster
  project: kenobi
  source:
    chart: base
    repoURL: https://istio-release.storage.googleapis.com/charts
    targetRevision: 1.13.1
    helm:
      releaseName: istio-base
      values: |-
