apiVersion: argoproj.io/v1alpha1
kind: Application

metadata:
  name: kenobi-istio-system-istio-gateway
  namespace: argocd

spec:
  destination:
    namespace: istio-system
    name: in-cluster
  project: kenobi
  source:
    chart: gateway
    repoURL: https://istio-release.storage.googleapis.com/charts
    targetRevision: 1.13.1
    helm:
      releaseName: istio-gateway
      values: |-
