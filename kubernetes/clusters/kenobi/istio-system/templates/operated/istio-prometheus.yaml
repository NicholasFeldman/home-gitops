apiVersion: monitoring.coreos.com/v1
kind: Prometheus

metadata:
  name: istio-prometheus

spec:
  storage:
    volumeClaimTemplate:
      spec:
        resources:
          requests:
            storage: 10Gi
