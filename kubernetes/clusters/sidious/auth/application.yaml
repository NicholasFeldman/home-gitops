apiVersion: argoproj.io/v1alpha1
kind: Application

metadata:
  name: sidious-auth
  namespace: argocd

spec:
  destination:
    namespace: auth
    name: in-cluster
  project: sidious
  source:
    path: kubernetes/clusters/sidious/auth
    repoURL: git@github.com:NicholasFeldman/kubernetes-config.git
    targetRevision: HEAD
  syncPolicy:
    automated:
      prune: true
      selfHeal: true
