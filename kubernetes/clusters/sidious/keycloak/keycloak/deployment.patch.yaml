apiVersion: apps/v1
kind: Deployment
metadata:
  name: keycloak

  template:
    spec:
      containers:
        - name: keycloak
          envFrom:
            - secretRef:
                name: keycloak-env
