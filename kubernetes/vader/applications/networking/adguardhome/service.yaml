apiVersion: v1
kind: Service
metadata:
  name: adguardhome-service
  namespace: database


spec:
  type: ClusterIP
  selector:
    app: adguardhome
  ports:
    - name: http
      port: 80
      targetPort: http
