apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: organizr-config-pvc
  namespace: user-apps


spec:
  storageClassName: longhorn-durable
  accessModes:
    - ReadWriteOnce

  resources:
    requests:
      storage: 256Mi

  volumeName: user-apps-organizr-config-pv
