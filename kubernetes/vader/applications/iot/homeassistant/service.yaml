apiVersion: v1
kind: Service
metadata:
  name: homeassistant-service
  namespace: iot

spec:
  type: ClusterIP
  selector:
    app: homeassistant
  ports:
    - name: http
      port: 80
      targetPort: http
