apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

metadata:
  name: flidd

resources:
  - fluidd.virtualservice.yaml

helmChartInflationGenerator:
  - chartName: app-template
    chartVersion: 1.3.2
    chartRepoUrl: http://bjw-s.github.io/helm-charts/
    releaseName: fluidd
    values: values.yaml
