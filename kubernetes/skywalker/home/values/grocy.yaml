image:
  repository: linuxserver/grocy
  tag: 4.0.0
env:
  TZ: America/New_York
service:
  main:
    ports:
      http:
        port: 80
envFrom:
  - secretRef:
      name: grocy-env
persistence:
  data:
    enabled: true
    mountPath: /config
    accessMode: ReadWriteOnce
    size: 4Gi