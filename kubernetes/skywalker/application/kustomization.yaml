apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

metadata:
  name: application

resources:
  - application.namespace.yaml

  - dashy
  - fluidd
  - grocy
  - home-assistant
  - keycloak
  - mealie
  - node-red
  - openldap
  - snipe-it
