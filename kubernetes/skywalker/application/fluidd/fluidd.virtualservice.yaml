apiVersion: networking.istio.io/v1beta1
kind: VirtualService

metadata:
  name: fluidd

spec:
  gateways:
    - cluster/feldman-in
  hosts:
    - fluidd.feldman.in
  http:
    - match:
        - uri:
            prefix: /
      route:
        - destination:
            host: fluidd
            port:
              number: 80
