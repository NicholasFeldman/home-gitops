apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

metadata:
  name: homepage

resources:
  - homepage-edit.virtualservice.yaml
  - homepage.virtualservice.yaml

helmCharts:
  - name: app-template
    repo: https://bjw-s-labs.github.io/helm-charts
    releaseName: homepage
    version: 1.5.1
    valuesFile: homepage.values.yaml
    apiVersions:
      - gateway.networking.k8s.io/v1beta1/HTTPRoute
