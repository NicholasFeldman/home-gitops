apiVersion: argoproj.io/v1alpha1
kind: Application

metadata:
  name: skywalker-metallb-system-metallb
  namespace: argocd

spec:
  destination:
    namespace: metallb-system
    name: in-cluster
  project: skywalker
  source:
    chart: metallb
    repoURL: https://metallb.github.io/metallb
    targetRevision: 0.13.9
    helm:
      releaseName: metallb
      values: |-
        configInline:
          address-pools:
            - name: default
              protocol: layer2
              addresses:
                - 10.10.90.100-10.10.90.109
