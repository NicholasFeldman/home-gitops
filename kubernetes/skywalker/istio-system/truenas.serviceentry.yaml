apiVersion: networking.istio.io/v1alpha3
kind: ServiceEntry

metadata:
  name: truenas

spec:
  hosts:
    - truenas.feldman.in
  addresses:
    - 10.10.90.30
  ports:
    - number: 80
      name: http
      protocol: HTTP
  resolution: STATIC
  location: MESH_EXTERNAL