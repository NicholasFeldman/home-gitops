apiVersion: networking.istio.io/v1alpha3
kind: VirtualService
metadata:
  name: truenas
spec:
  gateways:
    - istio-system/feldman-in
  hosts:
    - truenas.feldman.in
  http:
  - route:
      - destination:
          host: truenas
        weight: 100
