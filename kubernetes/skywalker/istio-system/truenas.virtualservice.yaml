apiVersion: networking.istio.io/v1alpha3
kind: VirtualService
metadata:
  name: httpbin-ext
spec:
  gateways:
    - istio-system/feldman-in
  hosts:
    - truenas.feldman.in
  http:
  - route:
      - destination:
          host: 10.10.90.30
        weight: 100
