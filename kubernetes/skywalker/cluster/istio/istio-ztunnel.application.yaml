apiVersion: argoproj.io/v1alpha1
kind: Application

metadata:
  name: skywalker-cluster-istio-ztunnel
  namespace: cluster

spec:
  destination:
    namespace: cluster
    name: in-cluster
  project: skywalker
  source:
    chart: istiod
    repoURL: https://istio-release.storage.googleapis.com/charts
    targetRevision: 1.18.2
    helm:
      releaseName: ztunnel
