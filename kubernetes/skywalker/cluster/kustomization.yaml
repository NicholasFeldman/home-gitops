apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

metadata:
  name: cluster

resources:
  - cluster.namespace.yaml
  - cluster.limitrange.yaml
  - gateway-api.yaml

  - argocd
  - cert-manager
  - metallb
