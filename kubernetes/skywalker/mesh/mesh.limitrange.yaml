apiVersion: v1
kind: LimitRange

metadata:
  name: mesh-limit-range

spec:
  limits:
  - type: Container
    default:
      memory: 256Mi
      cpu: 500m
    defaultRequest:
      memory: 64Mi
      cpu: 50m
