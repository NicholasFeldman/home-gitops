apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

metadata:
  name: postgresql

resources:
  - postgresql.application.yaml
  - postgresql.sealed-secret.yaml
  - postgresql.virtual-service.yaml
