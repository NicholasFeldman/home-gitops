apiVersion: argoproj.io/v1alpha1
kind: Application

metadata:
  name: skywalker-database-postgresql
  namespace: cluster

spec:
  destination:
    namespace: database
    name: in-cluster
  project: skywalker
  source:
    chart: postgresql
    repoURL: https://charts.bitnami.com/bitnami
    targetRevision: 11.9.*
    helm:
      releaseName: postgresql
      values: |-
        image:
          tag: 14.9.0-debian-11-r57
        auth:
          username: nick
          database: default
          existingSecret: postgresql
        primary:
          persistence:
            size: 16Gi
          livenessProbe:
            enabled: false
