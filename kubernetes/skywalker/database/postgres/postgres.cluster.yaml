apiVersion: postgresql.cnpg.io/v1
kind: Cluster

metadata:
  name: test-cluster

spec:
  instances: 3

  storage:
    storageClass: longhorn-single-replica
    size: 1Gi

  monitoring:
    enablePodMonitor: true
