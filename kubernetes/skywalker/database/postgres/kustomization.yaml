apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

metadata:
  name: postgres

resources:
  - postgres-s3.sealed-secret.yaml
  - postgres.cluster.yaml
  - test-cluster.virtualservice.yaml
