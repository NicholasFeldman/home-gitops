apiVersion: argoproj.io/v1alpha1
kind: Application

metadata:
  name: skywalker-application-kube-prometheus
  namespace: cluster

spec:
  destination:
    namespace: operator
    name: in-cluster
  project: skywalker
  source:
    chart: kube-prometheus
    repoURL: https://charts.bitnami.com/bitnami
    targetRevision: 8.22.0
    helm:
      releaseName: kube-prometheus
      values: |-
