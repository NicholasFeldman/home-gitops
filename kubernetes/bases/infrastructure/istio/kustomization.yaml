apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

helmCharts:
  - releaseName: base
    name: base
    version: 1.12.0
    repo: https://istio-release.storage.googleapis.com/charts
    includeCRDS: true
  - releaseName: istiod
    name: istiod
    version: 1.12.0
    repo: https://istio-release.storage.googleapis.com/charts
    includeCRDS: true