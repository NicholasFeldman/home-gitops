apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

helmCharts:
  - releaseName: base
    name: traefik
    version: 10.6.2
    repo: https://istio-release.storage.googleapis.com/charts
    includeCRDS: true
