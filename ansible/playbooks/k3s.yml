- hosts: server
  gather_facts: yes
  become: yes
  roles:
    - role: k3s
  vars:
    k3s_role: server
    extra_server_args: --disable traefik --disable servicelb

- hosts: agent
  gather_facts: yes
  become: yes
  roles:
    - role: k3s
  vars:
    k3s_role: agent
